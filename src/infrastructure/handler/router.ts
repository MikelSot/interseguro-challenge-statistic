import RouteSpecification from '@model/route-specification'

import NewRouter from './statistic/router'

const InitRouter = (spec: RouteSpecification) => {
  // S
  NewRouter(spec)
}

export default InitRouter
